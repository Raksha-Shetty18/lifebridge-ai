# Server Configuration
PORT=5000

# Google Gemini API Key (Optional: system provides rich intelligent fallback if not configured)
GEMINI_API_KEY=

# Supabase Configuration (Optional: system provides seamless local storage fallback)
SUPABASE_URL=
SUPABASE_ANON_KEY=
